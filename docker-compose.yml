services:
  backend:
    build: ./snapurl-backend
    ports:
      - "${BACKEND_PORT}:5000"
    environment:
      - MONGO_URI=${MONGO_URI}

  frontend:
    build: ./snapurl-frontend
    ports:
      - "${FRONTEND_PORT}:80"
    depends_on:
      - backend