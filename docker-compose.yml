services:
  backend:
    build: ./snapurl-backend
    ports:
      - "5001:5000"
    environment:
      - MONGO_URI=${MONGO_URI}

  frontend:
    build: ./snapurl-frontend
    ports:
      - "80:80"
    depends_on:
      - backend