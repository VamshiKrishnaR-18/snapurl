apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: snapurl-ingress
spec:
  rules:
    - host: snapurl.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 5173